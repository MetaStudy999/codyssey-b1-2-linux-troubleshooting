# [Bug] OOM - MemoryGuard에 의한 프로세스 종료

> 상태: **실험 전 템플릿** — 대괄호 안내를 실제 측정 결과로 교체하세요.

## 1. Description (현상 설명)

- 발생 시각: `[YYYY-MM-DD HH:MM:SS TZ]`
- 실행 환경: `[OS / CPU / RAM / 아키텍처]`
- PID: `[PID]`
- 적용 설정: `MEMORY_LIMIT=[값]`
- 관측 현상: `[무엇이 어떻게 발생했는지]`

## 2. Reproduction (재현 절차)

1. `source "$HOME/agent-lab/env.sh"`
2. `export MEMORY_LIMIT=[Before 값]`
3. `bash scripts/run-agent.sh`
4. 별도 터미널에서 관제와 진단 명령 실행
5. `[장애 발생 조건과 경과 시간]`

## 3. Evidence & Logs (객관적 증거)

- [ ] 시간에 따른 RSS 증가가 3개 이상의 시점으로 보인다.
- [ ] 종료 직전 MemoryGuard·SELF-TERMINATED 로그가 있다.
- [ ] `MEMORY_LIMIT` 변경 전후를 최소 2회 실행했다.

### 관제 데이터 발췌

```text
[실제 monitor CSV의 핵심 구간을 붙여 넣기]
```

### 애플리케이션 로그 발췌

```text
[실제 로그의 직전·발생·직후 구간을 붙여 넣기]
```

### 시스템 명령 출력

```text
[실제로 실행한 명령과 출력 붙여 넣기]
```

- 스크린샷: `[evidence/... 상대경로]`

## 4. Root Cause Analysis (원인 분석)

- 관측 사실: `[로그와 수치로 직접 확인한 내용]`
- 원인 추론: `[관측에서 원인으로 이어지는 논리]`
- 관련 OS 원리: `[메모리/스케줄링/락 동작]`
- 다른 가능성을 배제한 근거: `[반증 또는 추가 확인]`

## 5. Workaround & Verification (조치 및 검증)

- 변경 전: `MEMORY_LIMIT=[Before]`
- 변경 후: `MEMORY_LIMIT=[After]`
- 다른 실행 조건: `[동일함/차이가 있다면 기록]`

| 지표 | Before | After |
| --- | ---: | ---: |
| 시작 시각 | [값] | [값] |
| PID | [값] | [값] |
| 생존 시간 | [값] | [값] |
| 최대 CPU | [값]% | [값]% |
| 최대 RSS | [값] KB | [값] KB |
| 종료·무응답 여부 | [값] | [값] |

검증 결론: `[조치가 무엇을 바꾸었고 무엇은 해결하지 못했는지]`

## 6. 근본 해결 제안

- 임시 조치의 한계: `[환경변수 조정의 한계]`
- 코드 수준 개선: `[구체적 제안]`
- 운영 모니터링 개선: `[경고 임계치·추세·알림]`

## 7. 제출 확인

- [ ] 실제 값으로 모든 안내 문구를 교체했다.
- [ ] PID와 타임스탬프가 자료 사이에서 일치한다.
- [ ] Before & After를 같은 조건에서 측정했다.
- [ ] 민감정보를 제거했다.
