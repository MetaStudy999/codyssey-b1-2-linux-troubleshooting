# B1-2 리눅스 트러블슈팅 평가 문항

> OOM, CPU Spike, Deadlock 분석 결과와 트러블슈팅 역량을 확인하기 위한 평가 문항입니다.

## 평가 항목 한눈에 보기

| 항목 | 평가 영역 | 주요 내용 |
| --- | --- | --- |
| 1 | 결과물 및 증거 | 장애 재현, 환경변수 조정, Before & After, 리포트 형식 |
| 2 | 도구 활용 및 진단 과정 | `monitor.sh`, CPU 관제 도구, 무응답 프로세스 진단 |
| 3 | 기술 원리 이해 | 메모리 보호, CPU 과점유, Deadlock 발생 원리 |
| 4 | 실무 적용 및 심화 사고 | 모니터링 개선, 우선순위 판단, 코드 개선 |
| 5 | 보너스 | 보너스 문제 해결에 따른 추가 크레딧 |

---

## 항목 1. 결과물 및 객관적 증거

### OOM

- [ ] 메모리 사용량이 선형적으로 증가하다가 프로세스가 강제 종료되는 패턴이 로그에 기록되어 있는가?
- [ ] 환경변수 `MEMORY_LIMIT` 조정 후 프로세스 생존 시간이 늘어난 **Before & After** 비교 결과가 있는가?

### CPU

- [ ] CPU 사용률이 임계치를 초과하여 프로세스가 종료되는 패턴이 로그에 기록되어 있는가?
- [ ] 환경변수 `CPU_MAX_OCCUPY` 조정 후 프로세스 종료 여부 또는 생존 시간이 변화한 **Before & After** 비교 결과가 있는가?

### Deadlock

- [ ] 프로세스가 살아 있지만(PID 존재), CPU/메모리 변화 없이 로그가 멈춘 상태를 식별했는가?
- [ ] 환경변수 `MULTI_THREAD_ENABLE` 조정 후 데드락 재현/회피 비교 결과가 있는가?

### 리포트 형식 및 증거

- [ ] 3건의 리포트가 모두 GitHub Issue 구조인 **현상 → 증거 → 원인 → 조치**를 갖추고 있는가?
- [ ] 각 리포트에 PID, 로그 타임스탬프, 핵심 로그 메시지를 확인할 수 있는 증거가 첨부되어 있는가?
  - 스크린샷
  - 로그 발췌

## 항목 2. 도구 활용 및 진단 과정

- [ ] `monitor.sh`에서 메모리 증가 패턴을 추적하기 위해 사용한 명령어와 데이터 추출 방법을 구체적으로 설명할 수 있는가?
- [ ] 프로세스의 CPU 사용률을 확인하기 위해 선택한 도구와 적용한 옵션의 의미를 구분하여 설명할 수 있는가?
- [ ] 프로세스가 **“살아 있지만 멈춰 있는 상태”**임을 진단하기 위해 어떤 도구를 어떤 순서로 사용했는지, 판단 흐름을 논리적으로 제시할 수 있는가?

## 항목 3. 기술 원리 이해

### 메모리

- [ ] 메모리 누수가 발생했을 때 애플리케이션의 메모리 보호 정책이 해당 프로세스를 강제 종료하는 이유를 설명할 수 있는가?

### CPU

- [ ] CPU 과점유 시 단일 프로세스를 종료하는 것이 시스템 보호에 필요한 이유와 근거를 제시할 수 있는가?

### Deadlock

- [ ] 교착상태(Deadlock)가 발생하는 원리를 **상호 배제(Mutual Exclusion)**와 **순환 대기(Circular Wait)** 개념으로 설명할 수 있는가?
- [ ] 로그에서 스레드 간 순환 의존 관계(`A → B`, `B → A`)를 어떻게 파악했는지 추적 과정을 설명할 수 있는가?

## 항목 4. 실무 적용 및 심화 사고

### 1. 모니터링 개선

> 이번 미션의 `agent-leak-app`이 실제 운영 서버에서 동작하고 있었다면, 메모리 누수를 장애 발생 전에 탐지하기 위해 현재의 `monitor.sh`를 어떻게 개선하겠는가?

### 2. 장애 영향도 및 예방

> 이번 미션에서 경험한 세 가지 장애(OOM, CPU Spike, Deadlock) 중 실제 서비스 환경에서 가장 치명적인 장애는 무엇이라고 생각하는가?

- 판단한 이유를 설명할 수 있는가?
- 해당 장애를 근본적으로 예방하는 방법을 제안할 수 있는가?

### 3. 복합 장애 대응 우선순위

> 동일한 서버에서 OOM과 Deadlock이 동시에 발생했다면 어떤 순서로 트러블슈팅을 진행하겠는가?

- 대응 순서를 제시할 수 있는가?
- 우선순위를 결정한 근거를 설명할 수 있는가?

### 4. 코드 수준의 근본적 개선

> 이번 미션의 환경변수 조정은 임시 조치였다. 소스 코드를 직접 수정할 수 있다면 각 장애 유형을 어떻게 개선하겠는가?

| 장애 유형 | 코드 수준 개선 방안 |
| --- | --- |
| OOM / Memory Leak |  |
| CPU Spike |  |
| Deadlock |  |

### 5. 회고

> 이 미션을 처음부터 다시 수행한다면 트러블슈팅 과정에서 어떤 점을 다르게 접근하겠는가?

## 항목 5. 보너스

> [!NOTE]
> 보너스 문제 해결 결과에 따라 추가 크레딧을 부여합니다.
